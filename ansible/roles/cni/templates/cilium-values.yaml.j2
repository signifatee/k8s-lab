kubeProxyReplacement: true
k8sServiceHost: "{{ hostvars[groups['control_plane'][0]].ansible_host }}"
k8sServicePort: 6443
ipam:
  mode: kubernetes
operator:
  replicas: 1
hubble:
  relay:
    enabled: true
  ui:
    enabled: true